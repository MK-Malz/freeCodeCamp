function telephoneCheck(str) {

  var regExAlpha = /[a-zA-Z]/ig
  if(str.match(regExAlpha))
  {
    return false;
  }
  var regExSpecC = /[^-^(^)0-9\s]/ig
  if(str.match(regExSpecC))
  {
    return false;
  }


  return true;
}

telephoneCheck("555-555-5555");
